/**
 * The default settings for Slapshot.
 */
const DEFAULT_SETTINGS = {
  camera_angle: 40,
  camera_distance: 45,
  fps: false,
  ghost: false,
  minimap: true,
  mouse_sensitivity: 50,
  mouse_y: false,
  ping: false,
  shaders: true,
  volume: 50,
  controls: {},
  public_chat: true,
  low_gpu: false,
  movement_deadzone: 0.15,
};

export default DEFAULT_SETTINGS;
